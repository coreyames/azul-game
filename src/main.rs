enum TileKind {
    Red,
    Yellow,
    Blue,
    Black,
    White,
    BLANK, 
}

struct Board {
    quilt: Vec<TileKind>,
    working: Vec<TileKind>
}

impl Board {

}



fn main() {
    let mut board1 = Board {
        quilt
    }
}
